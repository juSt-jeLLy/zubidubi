{
  "name": "zubidubi-subgraph",
  "version": "0.9.3",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:studio": "graph deploy ${SUBGRAPH_SLUG:-zubidubi} subgraph.yaml --version-label ${SUBGRAPH_VERSION:-v0.9.4}",
    "query:live": "node scripts/query-live.mjs"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.97.1",
    "@graphprotocol/graph-ts": "^0.38.0",
    "dotenv": "^16.4.7"
  }
}
